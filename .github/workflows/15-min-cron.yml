name: 15-minute-cron
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call the hackernews bot
        run: |
          curl --request POST \
          --url 'https://showwcase-cd6bqwlnj-kokanek.vercel.app/api/bot'