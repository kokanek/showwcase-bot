name: 15-minute-cron
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call the hackernews bot
        run: |
          curl --request POST \
          --url 'https://showwcase-bot.vercel.app/api/bot'